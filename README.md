# Osaka Gateway 2026 🇯🇵

Веб-платформа для инвестиционного проекта "Osaka Gateway 2026".
Сочетает в себе инвестиционный дашборд и туристический лендинг в стиле Modern Japan / Cyberpunk.

**Стек технологий:** React, TypeScript, Tailwind CSS, Vite, Recharts, Lucide.

---

## 🚀 Деплой на Timeweb.cloud App Platform

Этот проект полностью подготовлен для развертывания через **Docker**.

### Структура для деплоя
В проекте настроены необходимые конфигурационные файлы:
1.  **Dockerfile**: Мультистейдж сборка. Сначала собирает React-приложение через Node.js, затем упаковывает статику в Nginx.
2.  **nginx.conf**: Конфигурация веб-сервера для корректной работы SPA (React Router) и обработки 404 ошибок.
3.  **package.json & vite.config.ts**: Скрипты для продакшн-сборки (`npm run build`).

### Пошаговая инструкция

#### 1. Подготовка репозитория
Убедитесь, что все файлы проекта (включая `Dockerfile` и `nginx.conf`) загружены в ваш Git-репозиторий (GitHub/GitLab/Bitbucket).

#### 2. Создание приложения в Timeweb Cloud
1. Зайдите в панель управления **Timeweb Cloud**.
2. Перейдите в раздел **Apps (App Platform)**.
3. Нажмите **"Создать приложение"** (Create App).
4. Выберите источник: **GitHub** (или ваш Git-провайдер).
5. Выберите репозиторий `osaka-gateway-2026`.

#### 3. Настройка сборки (Configuration)
Timeweb часто автоматически определяет Dockerfile, но лучше проверить настройки:

*   **Тип приложения (Build Method):** `Dockerfile`
*   **Контекст (Context):** `/` (корневая папка)
*   **Dockerfile path:** `Dockerfile`
*   **Регион:** Выберите ближайший (например, ru-1).

#### 4. Настройка переменных (Environment)
*Для этого проекта переменные окружения не требуются, так как API-ключи не используются, а конфиг зашит в сборку.*

#### 5. Выбор ресурсов и Деплой
1. Выберите подходящий тариф (для React+Nginx хватит минимального тарифа, например, **Start** или **M**).
2. Нажмите **"Запустить деплой"** (Deploy App).

### Проверка работоспособности
После успешной сборки (это займет 1-3 минуты):
1. Timeweb выдаст вам технический домен (например, `app-xxxx.timeweb.cloud`).
2. Перейдите по ссылке.
3. Проверьте переходы по страницам:
    *   `/` (Инвесторам)
    *   `/tourist` (Туристам)
    *   `/economy` (Экономика)
4. Попробуйте обновить страницу на любом внутреннем роуте (например, `/economy`). Если страница не выдает 404 ошибку от Nginx, значит `nginx.conf` работает корректно.

---

## 🛠 Локальный запуск

Если вы хотите протестировать проект локально перед деплоем:

1. **Установка зависимостей:**
   ```bash
   npm install
   ```

2. **Запуск сервера разработки:**
   ```bash
   npm run dev
   ```
   Приложение будет доступно по адресу `http://localhost:3000`.

3. **Тест продакшн-сборки локально:**
   ```bash
   npm run build
   npm run preview
   ```

---

## 📁 Структура проекта

*   `components/` — Переиспользуемые UI компоненты (карточки, навбар, формы).
*   `pages/` — Основные страницы приложения.
*   `types.ts` — TypeScript интерфейсы.
*   `Dockerfile` — Инструкции для Docker сборки.
*   `nginx.conf` — Настройки сервера.

---
*Osaka Gateway 2026 Project. Confidential.*
